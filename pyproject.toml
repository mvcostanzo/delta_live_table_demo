[project]
name = "delta-live-table-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pip-system-certs>=5.2",
]

[tool.uv.workspace]
members = [
    "PopulateTable",
]
